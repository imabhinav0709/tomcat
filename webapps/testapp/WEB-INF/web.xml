<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <display-name>TestApp</display-name>

    <!-- Register authentication servlet for login processing. -->
    <servlet>
        <servlet-name>LoginServlet</servlet-name>
        <servlet-class>LoginServlet</servlet-class>
    </servlet>

    <!-- Register logout servlet for session termination. -->
    <servlet>
        <servlet-name>LogoutServlet</servlet-name>
        <servlet-class>LogoutServlet</servlet-class>
    </servlet>

    <!-- Register the StudentServlet class -->
    <servlet>
        <servlet-name>StudentServlet</servlet-name>
        <servlet-class>StudentServlet</servlet-class>
    </servlet>

    <!-- Define login URL endpoint handled by LoginServlet. -->
    <servlet-mapping>
        <servlet-name>LoginServlet</servlet-name>
        <url-pattern>/login</url-pattern>
    </servlet-mapping>

    <!-- Define logout URL endpoint handled by LogoutServlet. -->
    <servlet-mapping>
        <servlet-name>LogoutServlet</servlet-name>
        <url-pattern>/logout</url-pattern>
    </servlet-mapping>

    <!-- Map /students URL to StudentServlet -->
    <servlet-mapping>
        <servlet-name>StudentServlet</servlet-name>
        <url-pattern>/students</url-pattern>
    </servlet-mapping>

    <!-- Register authentication filter that protects private resources. -->
    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>AuthFilter</filter-class>
    </filter>

    <!-- Protect servlet endpoint for authenticated users only. -->
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/students</url-pattern>
    </filter-mapping>

    <!-- Protect JSP page for authenticated users only. -->
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/secure.jsp</url-pattern>
    </filter-mapping>

</web-app>
